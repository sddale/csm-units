#pragma once

namespace csm_units {

class DimLength {
  // friend constexpr long double operator""_M(long double data) {
  //   // Derived<Exponents<1, 0, 0>, Base<DimLength>, Base<DimMass>,
  //   Base<DimTime>,
  //   //         long double>;
  // Derived<Exponents<1, 0, 0>, Base<DimLength>, Base<DimMass>, Base<DimTime>,
  //         long double>(data);
  // }

  // auto lenMeter = 100.0_M;  // why is this not working??
};

}  // namespace csm_units
